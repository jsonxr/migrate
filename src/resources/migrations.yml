##############################################################################
# Auto generated.  This table is required to make the migration utility work.
##############################################################################

name: migrations
columns:
    - name: version
      type: varchar(255)
      nullable: false
      key: true
    - name: yml
      type: longtext
      nullable: false
    - name: applied_by
      type: string ###### Needs to be varchar(255) why aren't tests failing here
    - name: applied_on
      type: datetime
      nullable: false
