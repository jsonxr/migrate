previous: ~
commands:
    - { name: drop_table, table: migration_old }
    - { name: rename_table, table: migration_bak, old: migration }
    - { name: create_table, table: migration }
    - { name: add_column, table: altered_table, column: added_column }
    - { name: rename_column, table: altered_table, column: renamed_column, old: old_column }
    - { name: drop_column, table: altered_table, column: dropped_column }
    - { name: change_column, table: altered_table, column: changed_column }